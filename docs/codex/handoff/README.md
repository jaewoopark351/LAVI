<!-- #20260716_kpopmodder: Documents the approved handoff-pack copy location. -->

# Codex Handoff Pack Copies

This directory contains repository-local reference copies from:

```text
logs/LAVI_Codex_Handoff_Pack_v2
```

The source pack remains under `logs/` and is still treated as an external handoff
artifact. These copies are kept under `docs/codex/handoff/` so future agents can
read prompts, templates, and P0/P1 implementation notes without copying over
existing root-level project documentation.
